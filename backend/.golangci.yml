run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - gosimple

issues:
  exclude-dirs:
    - internal/service/auth/mocks
    - internal/service/crud/mocks
